# 引継ぎメモ — 2026-08-17 作業終了時点

次に作業を再開するときは、このファイルの上から順に読めば状況がわかるようにしてあります。

**締切:2026-08-25(TOPページ公開)= 残り8日**

---

## 今日やったこと(2026-08-17)

HOW TO ENJOY 01・02 のレイアウト問題(前回の①②)を解決し、**2件ともコミット済み**。
**ブラウザでの表示確認も済ませて問題なし**。

| コミット | 内容 |
|---|---|
| `f5c2265` | テキスト列を460pxに拡大、`@media (max-width:900px)` を新設して縦積みに |
| `da18867` | 写真を固定高さから `aspect-ratio: 16 / 9` へ |

### ① 説明文の折り返しを解消(案Aを採用)

グリッドの1列目を **380px → 460px**。`padding-left: 66px` を引いた文字幅が
314px → **394px** になり、いちばん長い行378pxが収まるようになった(余裕16px)。

あわせて `@media (max-width:1200px)` に残っていた `.how-to-enjoy__image--01 { width:70% }` /
`--02 { width:75% }` を削除。グリッドの列幅に任せることで、写真の右にできていた
宙に浮いた余白が消えた。

`@media (max-width: 900px)` を新設して 01・02 を縦積みに。中身は3つだけ
(`min-height:0` / `.how-to-enjoy__content { height:auto }` / `display:block`)。

> **メディアクエリの並び順は 1200 → 900 → 768。** 同じ強さの指定は後に書いたほうが
> 勝つので、900pxブロックを768pxブロックより後ろに動かしてはいけない。

### ② 写真の切り取りを解消

`height: 400px` / `350px`(固定px)を **`aspect-ratio: 16 / 9`** に置き換えた。

元画像は 01 が 732×412(1.777)、02 が 724×406(1.783)で、**16/9(1.778)とほぼ同じ**。
枠を同じ比率にしたので、どの画面幅でも切り取りがほぼゼロになった。
従来は1024px幅で 404×350 のほぼ正方形になり、横の約4割が切れていた。

`@media (max-width:1200px)` の `height: 350px` / `400px` も削除。基本側で比率を決めた以上、
特定の幅帯だけ固定pxに戻すと、その帯だけ切り取られ方が変わってしまう。

**現在の写真サイズ:**

| 画面幅 | 写真の枠 | レイアウト |
|---|---|---|
| 1440 | 660 × 371 | 横並び |
| 1200 | 580 × 326 | 横並び |
| 1024 | 404 × 227 | 横並び |
| 901 | 281 × 158 | 横並び(最小) |
| **900** | **820 × 461** | **縦積み** |
| 768以下 | 260 × 130(2:1) | 縦積み |

縦積みの境界は 1024px も試したが、405×228 → 944×531 と写真の変化が大きかったため
**900pxを採用**した。この経緯はCSSのコメントにも残してある。

スマホ(768px以下)の `aspect-ratio: 2 / 1` は実機確認済みの意図的な値なので**維持**。
PC側の比率に言及していた古いコメントだけ直した。

---

## 次にやること(優先順)

### ① 星座線を戻す ← 最優先

前回、説明文の上に星と線が重なるため**全画面幅で一時的に非表示**にしてある。
①②でレイアウトが確定したので、もう戻せる状態。

**戻し方**

[css/style.css](css/style.css) の **247〜267行目**、`【一時的に非表示】2026-08-16` の
コメントと、その下の1つのルールを**まとめて削除するだけ**。

```css
#constellation-svg,
#intro .star,
.star--enjoy01,
.star--enjoy02,
.star--enjoy03 {
   display: none;
}
```

スマホ用の非表示指定は `@media (max-width:768px)` の中(**2491行目付近**)に別で
残してあるので、消してもスマホの見え方は変わらない。

> **注意**:`display: none` にすると JS の `getBoundingClientRect()` が全て0を返し、
> 線が画面左上(0,0)に向かって描かれてしまう。だから星だけでなく
> `#constellation-svg` も一緒に隠してある。**片方だけ戻さないこと。**

**戻したあと、星の座標を調整する**

①でテキスト列が380px→460pxに広がったので、**写真の縁の位置が動いている**。
星は「写真の縁のすぐ外」に置く方針なので、今の座標のままだとズレる。

該当は [css/style.css](css/style.css) **279〜294行目**:

| セレクタ | 現在 | 写真の縁(1200px時) | 動かす向き |
|---|---|---|---|
| `.star--enjoy01` | `left: 36%` | 45%(写真は右side) | **右へ**(47%前後?) |
| `.star--enjoy02` | `left: 67%` | 55%(写真は左side) | **左へ**(57%前後?) |
| `.star--enjoy03` | `left: 8%` | 変更なし | そのままでよいはず |

**カッコ内は計算上の目安**。実際は目で見て詰めること。`top` は変えなくてよいはず。

### ② 背景画像3枚の書き出しやり直し

**10.8MB のうち背景3枚だけで 7.9MB。** Figmaの書き出しが `@2x` になっているのが原因で、
元画像に無い解像度は拡大しても増えないため、**重くなるだけで画質は上がらない**。

| ファイル | 今の実寸 | 今 | 書き出し幅 | 品質 | 目安 |
|---|---|---|---|---|---|
| `ticket.jpg` | 3344×1882 | 3.78MB | 1920px | 80 | 約400KB |
| `fv.jpg` | 2880×1700 | 2.18MB | 1920px | 80 | 約400KB |
| `modelcourse.jpg` | 2880×1200 | 1.90MB | 1920px | 80 | 約400KB |
| `howtoenjoy_01.jpg` | 732×412 | 0.42MB | **1100×619** | 80 | 約180KB |

→ **10.8MB → 約2MB**

`howtoenjoy_01.jpg` は逆に**小さすぎて**Retinaでぼやける。②で比率が **16/9** に決まったので、
**1100×619** で出し直せばよい(732×412 は 16/9 とほぼ同じ比率なので、
[index.html:148](index.html#L148) の `width="732" height="412"` を `1100` / `619` に直す)。

> **今あるJPEGを圧縮ツールにかけないこと。** 一度失った情報の上からもう一度圧縮することになり、
> 画質が二重に落ちる。**必ずFigmaの元データから出し直す。**

### ③ 判断待ちの設計2件(締切次第では見送ってよい)

#### `.section-title` の位置基準 ← こちらは目に付きやすい

`position: absolute; left: 130px` で、**基準がビューポート左端**になっている
(親の `.container` に `position` が無いため、基準は `position: relative` を持つ `section`)。

- `#intro` だけ `@media (max-width:1200px)` で `left: 40px` に直してある
- `#how-to-enjoy` / `#news` / `#access` / `#ticket` は `left: 130px` のまま
- → **1024px幅で INTRO だけ本文と揃い、他の4つが90px右にずれる**
- `left: 130px` は1440pxでちょうど良い値なので、**PCでも画面幅によって揃い位置が動く**

**根本解決**:`.container` に `position: relative` を足して、基準をコンテナに移す。
そうすれば `left: 0` 付近の値だけで全幅で揃う。
**PCの見え方も変わるので、1440pxで確認してから判断すること。**

#### スマホの `.container { max-width: 260px }`

`@media (max-width:768px)` 内。**768px幅(iPad縦)でも中身が260pxの柱**になり、
iPhone 15 Pro Max(430px)でも画面の6割しか使っていない。

変えるなら、**260px に依存している4箇所を同時に直す必要がある**:

1. `.how-to-enjoy__gallery--03` の `padding-inline: calc(50vw - 130px)`
2. 同 `scroll-padding-inline: calc(50vw - 130px)`
3. `.mobile-cta__button` の `max-width: 260px`
4. `.footer__content > *` の `max-width: 260px`

→ `--sp-col: 260px` のようなCSS変数を1つ作って、4箇所ともそれを参照させるのが安全。

### ④ その他(手つかず)

- **reveal footer 効果** — `main { position: relative; z-index: 1; background-color: ... }` +
  `footer { position: sticky; bottom: 0; }`。`position: sticky` はコード内に一切無い
- **施設名の表記ゆれ** — 「なみね水族館」に統一する。スープカップの写真・地図ピンなどに
  「波音水族館」が残っている(画像内の文字なので書き出しやり直しが必要)
- **スタイルガイドとコードの数値のズレ** — ボタン枠線色 `#35586B` vs 実装 `#183B4E`、
  `.section-title` の font-weight 700 vs ガイドのRegular など。**実装を正とし、ガイド側を更新する**
- **`event__image.jpg` の命名** — NEWSセクションの画像なのに、他が `news_` `howtoenjoy_`
  `modelcourse_` とセクション名で始まっているのに対して浮いている。`news_event.jpg` に戻すと揃う

---

## まだ見ていない表示(公開前に必ず)

- **スマホ実機** — `100svh` / ハンバーガーの視認性 / メニューの背面スクロール停止 /
  外タップで閉じる / 下部固定CTAの出入り
- **下層4ページ** — `news.html` / `event.html` / `model-course.html` / `ticket.html` の全幅
- **1440px以上の広い画面**
- ハンバーガーの不透明度 `0.6` は仮の値。目で見て詰めること

1日は見ておきたいので、日程に組み込むこと。
