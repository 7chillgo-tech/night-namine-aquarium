# 引継ぎメモ — 2026-08-16 作業終了時点

次に作業を再開するときは、このファイルの上から順に読めば状況がわかるようにしてあります。

---

## 明日やること(優先順)

### ① HOW TO ENJOY 01・02 の説明文が折り返される問題 ← 最優先

**現象**

PC・タブレット幅で、説明文が意図しない位置で折り返される。

```
大水槽の前や、お気に入りの水槽のそ
ばには、
```

**原因(計測済み)**

| 項目 | 値 |
|---|---|
| グリッドの1列目(テキスト列) | `380px` |
| 説明文の `padding-left` | `66px` |
| 実際に文字が使える幅 | **314px** |
| 説明文の `font-size` | `18px` |
| いちばん長い行「大水槽の前や、お気に入りの水槽のそばには、」 | 21文字 = **378px** |

378px の行を 314px の箱に入れているので、**64px 分あふれて折り返される**。
日本語は全角1文字がほぼ font-size と同じ幅になるので、`文字数 × 18px` で計算できる。

各行の必要幅:

| 行 | 文字数 | 必要幅 |
|---|---|---|
| 大水槽の前や、お気に入りの水槽のそばには、 | 21 | 378px |
| 少しだけでも、閉館までゆっくりでも。 | 18 | 324px |
| 椅子とテーブルをご用意しています。 | 17 | 306px |
| 昼とは違う姿を、のぞいてみませんか。(02) | 18 | 324px |

→ **最低 378px、余裕をみて 390px** の文字幅が必要。

**該当箇所**

- [css/style.css](css/style.css) `.how-to-enjoy__item--01 .how-to-enjoy__content`(`grid-template-columns: 380px 1fr`)
- 同 `.how-to-enjoy__item--02 .how-to-enjoy__content`(`grid-template-columns: 1fr 380px`)
- 同 `.how-to-enjoy__description--01, --02`(`padding-left: 66px`)

**選択肢(明日ここを決める)**

#### 案A:テキスト列を広げる ◎ おすすめ

1. `grid-template-columns` の `380px` を **`460px`** にする(460 − 66 = 394px 確保)
2. `@media (max-width:1200px)` の `.how-to-enjoy__image--01 { width: 70% }` と
   `--02 { width: 75% }` を **削除**して、写真を列いっぱいに広げる
   (今は右側に何も無い余白ができている。スクショで確認済み)
3. **`@media (max-width:900px)` を新設して、01・02 を縦積みにする**
   - 460 + gap 56 + 写真 という3つを横に並べると、900px より狭い画面では写真が
     170px まで潰れる。そこから下は縦積みが正解
   - 縦積みの書き方は `@media (max-width:768px)` の中に既にあるので、
     `.how-to-enjoy__item--01 .how-to-enjoy__content { display: block }` を流用できる

幅ごとの計算(gap 56px、写真は列いっぱいの場合):

| 画面幅 | コンテナ | テキスト列 | 写真 | 判定 |
|---|---|---|---|---|
| 1280px〜 | 1200 | 460 | 684 | ◎ |
| 1024px | 944 | 460 | 428 | ○ |
| 900px | 820 | 460 | 304 | △ ここが限界 |
| 769px | 689 | 460 | 173 | ✕ 縦積みにする |

#### 案B:`padding-left: 66px` を外す

1行消すだけで文字幅が 380px になり、378px の行がぎりぎり収まる。
ただし **余裕が2pxしかない**ので、フォントの読み込み状況で折り返す危険がある。
また、この padding は「タイトルの文字( `.number-badge` 56px + margin 10px の右から始まる)と
説明文の左端を揃える」ためのものなので、**外すと左揃えが崩れる**。

#### 案C:説明文そのものを短くする

昨日の相談で出ていた案。文章を変えるので、コピーの検討が必要。
ただし **21文字は日本語のWebコピーとして長すぎるわけではない**ので、
「レイアウトが狭いから文章を削る」のは順序が逆になる。案Aで直すほうが素直。

---

### ② 写真の切り取られ方 ← ①とセットで

**現象**

写真が横に大きく切り取られている。

**原因**

- 元画像 `howtoenjoy_01.jpg` は **732×412(横長 1.78:1)**
- 表示枠は `width: 70%` × `height: 350px` で、画面幅によって比率が変わる
  (1024px幅のとき およそ 362×350 = ほぼ正方形 0.97:1)
- `object-fit: cover` が枠を埋めようとするので、**横の約45%が切り落とされている**

**対応の考え方**

`height: 350px`(固定px)をやめて **`aspect-ratio`** にすると、
どの画面幅でも同じ切り取られ方になって安定する。
スマホ用の指定([css/style.css](css/style.css) の `@media (max-width:768px)` 内)では
すでに `aspect-ratio: 2 / 1` を使っているので、同じ考え方をPC側にも広げる形。

**どの比率にするかはデザインの判断**。Figmaの枠の比率に合わせるのが確実。
比率が決まったら、その比率に近いサイズで画像を書き出し直すと、
切り落とすだけのピクセルを配信せずに済む。

---

### ③ 星座線を戻す

今日、**全画面幅で一時的に非表示**にした。
説明文の上に星と線が重なっていたため。

- 場所:[css/style.css](css/style.css) の「星座」セクション先頭、`【一時的に非表示】2026-08-16` のブロック
- **戻し方:そのブロック(ルール1つ)を削除するだけ**
- スマホ用の非表示指定は `@media (max-width:768px)` の中に別で残してあるので、
  消してもスマホの見え方は変わらない

**①②でレイアウトが動くと星の座標もずれる**ので、**①②を終えてから**位置を調整すること。
通過点の座標は `.star--enjoy01 / 02 / 03` の `top` / `left`(%指定)。

> 注意:`display: none` にすると JS の `getBoundingClientRect()` が全て0を返し、
> 線が画面左上(0,0)に向かって描かれてしまう。だから星だけでなく
> `#constellation-svg` も一緒に隠してある。片方だけ戻さないこと。

---

### ④ 背景画像3枚の書き出しやり直し

今日の圧縮で **17MB → 10.8MB** まで来たが、**背景3枚だけで 8.0MB** 残っている。

| ファイル | 今の実寸 | 今 | 元PNG | 問題 |
|---|---|---|---|---|
| `ticket.jpg` | 3344×1882 | 3870KB | 2314KB | 元の**ちょうど2倍**に拡大。PNGより重い |
| `fv.jpg` | 2880×1700 | 2229KB | 1937KB | 元の約1.7倍に拡大。PNGより重い |
| `modelcourse.jpg` | 2880×1200 | 1949KB | 3889KB | 半減したが実寸が過剰 |
| `howtoenjoy_01.jpg` | 732×412 | 427KB | 2006KB | 逆に**小さすぎ**。Retinaでぼやける |

**原因**:Figmaの書き出しが `@2x` になっている。
元画像に無い解像度は拡大しても増えないので、**ファイルが重くなるだけで画質は上がらない**。

**目標値**

| ファイル | 書き出し幅 | 品質 | 目安 |
|---|---|---|---|
| `fv.jpg` | 1920px | 80 | 約400KB |
| `ticket.jpg` | 1920px | 80 | 約400KB |
| `modelcourse.jpg` | 1920px | 80 | 約400KB |
| `howtoenjoy_01.jpg` | 1100px(②の比率が決まってから) | 80 | 約180KB |

→ **10.8MB → 約2MB**

ファイル名と比率が変わらなければ、**パスも `width`/`height` 属性もそのままで動く**。
比率を変えた場合は [index.html](index.html) の `width` / `height` を直すこと。

> 今あるJPEGを圧縮ツールにかけないこと。一度失った情報の上からもう一度圧縮することになり、
> 画質が二重に落ちる。**必ずFigmaの元データから出し直す**。

---

### ⑤ 判断待ちの設計2件(締切次第では見送ってよい)

#### スマホの `.container { max-width: 260px }`

[css/style.css](css/style.css) の `@media (max-width:768px)` 内。

`max-width: 768px` のメディアクエリなので、**768px幅(iPad縦)でも中身が260pxの柱**になる。
iPhone 15 Pro Max(430px)でも画面の6割しか使っていない。

変えるなら、**260px に依存している4箇所を同時に直す必要がある**:

1. `.how-to-enjoy__gallery--03` の `padding-inline: calc(50vw - 130px)`
2. 同 `scroll-padding-inline: calc(50vw - 130px)`
3. `.mobile-cta__button` の `max-width: 260px`
4. `.footer__content > *` の `max-width: 260px`

→ `--sp-col: 260px` のようなCSS変数を1つ作って、4箇所ともそれを参照させるのが安全。

#### `.section-title` の位置基準

`position: absolute; left: 130px` で、**基準がビューポート左端**になっている
(親の `.container` に `position` が無いため、基準は `position: relative` を持つ `section`)。

- `#intro` だけ `@media (max-width:1200px)` で `left: 40px` に直してある
- `#how-to-enjoy` / `#news` / `#access` / `#ticket` は `left: 130px` のまま
- → 1024px幅で **INTRO だけ本文と揃い、他の4つが90px右にずれる**
- さらに `left: 130px` は1440pxでちょうど良い値なので、**PCでも画面幅によって揃い位置が動く**

**根本解決**:`.container` に `position: relative` を足して、
`.section-title` の基準をコンテナに移す。そうすれば `left: 0` 付近の値だけで全幅で揃う。
**PCの見え方も変わるので、1440pxで確認してから判断すること。**

---

### ⑥ その他(手つかず)

- **reveal footer 効果** — `main { position: relative; z-index: 1; background-color: ... }` +
  `footer { position: sticky; bottom: 0; }`。`position: sticky` はコード内に一切無い
- **施設名の表記ゆれ** — 「なみね水族館」に統一する。スープカップの写真・地図ピンなどに
  「波音水族館」が残っている(画像内の文字なので書き出しやり直しが必要)
- **スタイルガイドとコードの数値のズレ** — ボタン枠線色 `#35586B` vs 実装 `#183B4E`、
  `.section-title` の font-weight 700 vs ガイドのRegular など。**実装を正とし、ガイド側を更新する**
- **`event__image.jpg` の命名** — NEWSセクションの画像なのに、他が `news_` `howtoenjoy_`
  `modelcourse_` とセクション名で始まっているのに対して浮いている。`news_event.jpg` に戻すと揃う

---

## 今日やったこと(2026-08-16)

### 表示崩れの修正

| # | 内容 |
|---|---|
| 1 | `@media (max-width:1200px)` に残っていた絶対配置時代の指定を削除。HOW TO ENJOY 01・02 の説明文が幅143px / 116pxまで潰れていた原因 |
| 2 | `.how-to-enjoy__image--02` の `left: 20%` を削除。写真が列の中で約100px右にずれていた |
| 3 | `.star { display: none }` を通過点3つに限定。NEWS見出しの装飾星まで消えていた |

**1・2の根本原因**:01・02 はもともと `position: absolute` で座標を直打ちしていたが、
後から CSS Grid に作り替えた。しかし `@media (max-width:1200px)` の中身が
絶対配置時代のまま残っていた。`left` / `top` は効かなくなっていたが `width` だけが効き続けていた。

### スマホ実機の対応

| # | 内容 |
|---|---|
| 4 | `#fv` / `#ticket` に `100svh` を追加(`100vh` はフォールバックとして残置)。URLバーで高さが変わる問題 |
| 5 | ハンバーガーに `header:not(.is-dark)` の濃い背景を追加。明るいセクションで白線が読めなかった |
| 6 | メニュー展開中の背面スクロールを停止(`body.is-menu-locked`) |
| 7 | メニュー外タップで閉じる処理を追加(スマホにEscキーが無いため) |

### 整合性・保守性

| # | 内容 |
|---|---|
| 8 | スマホの `scroll-margin-top` を 88px → 72px(ヘッダーの帯を無くしたため) |
| 9 | NEWS左列(EVENT)の文字を右列と同じ 12px / 13px に統一 |
| 10 | `.news__item` を `minmax(220px, auto)` に。news.html の長いタイトルが折れていた |
| 11 | `event.html` / `model-course.html` に `.mobile-cta` を追加 |
| 12 | 写真7枚に `loading="lazy"`。**ウツボと星座線の星は意図的に除外**(理由はコード内コメント) |
| 13 | 全画像に `width` / `height` 属性を追加(読み込み中のガタつき防止) |
| 14 | 装飾画像の `alt` を空に統一。CSSの重複・死にコード5箇所を削除 |
| 15 | JS:開閉処理を `setMenu(isOpen)` に一本化(開く/閉じるが別々で、片方に足し忘れる作りだった) |

### 画像の圧縮(ユーザー作業)

- **17MB → 10.8MB**
- ドリンク3枚を PNG のまま書き出し直し。`azarashi` 1990KB → 176KB、
  3枚とも 397×278 に統一(以前は縦横比がバラついて価格の行が数pxずれていた)
- 一度JPEGにしたが、**四隅が RGB(255,255,255) の白で埋まって透過が失われた**ため PNG に戻した
  (JPEGは透過を持てない形式。ブロブ型の切り抜きには使えない)
- 未使用だった `intro_img.png`(2.4MB)/ `intro_woman.png`(153KB)を削除
- `ticket.png` → `ticket.jpg`、`news_event.png` → `event__image.jpg` に差し替え、パス更新済み
- **参照切れ0件・未使用ファイル0件を確認済み**

---

## 確認できていないこと

- **ブラウザでの実表示は、1024px前後のHOW TO ENJOY しか見ていない**(スクリーンショットで確認)
- 未確認:**スマホ実機**(`100svh` / ハンバーガーの視認性 / メニューの背面スクロール停止 /
  外タップで閉じる / 下部固定CTAの出入り)
- 未確認:**下層4ページ**(`news.html` / `event.html` / `model-course.html` / `ticket.html`)の全幅
- 未確認:**1440px以上の広い画面**
- 5番のハンバーガーの不透明度 `0.6` は仮の値。目で見て詰めること
