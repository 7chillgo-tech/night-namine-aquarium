# 夜のなみね水族館 コーディング引き継ぎメモ

最終更新: 2026-08-07

## プロジェクト概要

- 就職活動用ポートフォリオサイト(2サイト作成予定のうちの1つ目)
- 架空の市営水族館「なみね水族館」の夜間営業リニューアルLP
- コンセプト:「帰り道に寄り道できる、何度でも来れて休める、癒される水族館」
- 都会型ナイトアクアリウムとの差別化:非日常より日常の延長、少人数のリピーター獲得、特別すぎない親しみやすさ
- 制作者の学習状況:HTML/CSS(XHTML・HTML5)は職業訓練で習得済み、JavaScriptはほぼ未経験(Progateで変数のみ独学)。CSS Grid, Sass, BEM, 複雑なJS, フレームワーク, 高度なアニメーション, APIは未学習の可能性が高い
- 締切:8/25までにサイト1・サイト2をそれぞれTOPページだけでも公開状態にする

## 使用ツール・環境

- エディタ: VS Code
- バージョン管理: GitHub Desktop → GitHub Pages
- デザイン: Figma
- リポジトリ: `7chillgo-tech/night-namine-aquarium`
- 公開URL: `https://7chillgo-tech.github.io/night-namine-aquarium/`

## デザインの決定事項

- フォント: 見出し・コピー系は明朝系検討中/本文は Zen Kaku Gothic New、英字ラベルは DM Sans
- 配色: 深紺 × クリーム(#183B4E, #F3F0E8 など)、グラデーション背景を導入(introで実装済み)
- モチーフ: 泡(気泡)装飾を採用予定(当初のキラキラ装飾から変更)。星座モチーフ(点線で繋ぐ)はモデルコースセクションで使用
- 写真の切り抜きルール:
  - 円形 = 生き物・展示にフォーカスする瞬間
  - ブロブ(いびつな角丸) = くつろぐ空間・軽食
  - フルブリード四角 = 情報・全景セクション
- 画像命名規則: `[セクション名]_[内容].拡張子`、小文字ケバブケース、日本語・スペース・バージョン語(final/v2等)禁止
- **クラス命名規則: BEM(`ブロック__要素--モディファイア`)に統一済み(2026-08-07)**。`.container` `.button` `.section-title` `.section-subtitle` `.star` `.number-badge` は特定ブロックに属さない共通コンポーネントとしてブロック名を付けずに使う。IDはアンカーリンク(`href="#intro"`等)とCSSのブロックルート選択に使うため維持し、リネーム対象外。

## デザイン確認について

Figma URL: `https://www.figma.com/design/KYDapGfPtkymdOVk7hudKo/night-namine-aquarium`(node-idはフレームごとに異なる)。このセッションではFigma MCP連携が未接続で、URLを渡してもClaude側からは内容を取得できなかった。**現状はスクリーンショットを貼ってもらう形で確認する運用。** MCP接続を試す場合は接続状況を都度確認すること。

## コーディング済み箇所

- **header**: `position: fixed`。スクロール量0で透明、スクロールすると`is-scrolled`クラスが付与され背景色(`rgba(24,59,78,0.6)`)+`backdrop-filter: blur(10px)`が付く。以前あった「スクロール中は消える」演出は可読性の問題で削除済み。クラスは`.header__logo` `.header__nav`。
- **#fv**: 完成。`height:100vh`、背景`fv.png`。コピー「かえりみち、海に寄り道。」「島の海と、ひと休み。」、右にイベントロゴ・開催期間画像。クラスは`.fv__title` `.fv__subtitle` `.fv__logo` `.fv__date`。FVとintroの繋ぎ目(波デザイン)は保留中(全体デザイン見直し中のため)。
- **#intro**: 完成。左にラベル(h2)+見出し(h3)+本文+ボタン(`.button`)、右に丸窓写真(`.intro__image`)+女性の写真(`.intro__image-sub`、Figmaで個別トリミング書き出し済み、`object-fit: cover; object-position: top;`)+星(`.star`、`data-star`属性でJSの星座線描画対象になっている)。背景は`radial-gradient`。
- **#how-to-enjoy**: 完成。3項目(`.how-to-enjoy__item`に`--01`〜`--03`のモディファイア)。各項目に`.how-to-enjoy__title`(`.number-badge`付き)、`.how-to-enjoy__image`、`.how-to-enjoy__description`。**CSSは未着手**(HTML構造のみ)。
- **#news**: デザイン変更を実施(詳細未共有)。中身は未実装。
- **#model-course**: HTML骨組みのみ存在(`.model-course__content` `.model-course__title` `.model-course__image` `.model-course__description`)。中身の実装はこれから。
  - Figmaデザイン: ラベル「03 | MODEL COURSE」、ハイライトされたコピー「会社帰りに、ひとり時間。」、ボタン「夜の過ごし方を見る→」、夜景の背景画像、時系列4ステップ(18:30/18:45/19:15/20:00)を線画アイコン(人物・魚・グラス・椅子)で表示し、点線(星座のように)で繋ぐ演出。
- **#news, #access, #ticket, footer**: 未実装、または一部進行中(newsはデザイン変更のみ)。

## アンカージャンプ時のスクロール位置

headerが`position: fixed; height: 88px;`のため、`#intro` `#how-to-enjoy` `#model-course` `#news` `#access` `#ticket`すべてに`scroll-margin-top: 88px;`を設定し、アンカージャンプ時に見出しがheaderの下に隠れないようにしている([css/style.css:37-45](../css/style.css)、2026-08-07に0px→88pxへ修正・動作確認済み)。

## 見出し構造のルール(合意済み)

```
h1: ヘッダーロゴ
h2: 各セクションのラベル(例: "01 | INTRODUCTION", "02 | HOW TO ENJOY")
h3: 各セクション内の具体的な見出し(例: "水槽の前で、ひとり時間。")
```
同じ階層の兄弟見出しは同じレベルのタグに揃える。

## 現在つまずいていること

1. **ヘッダーと背景にある線のJavaScriptが理解できていない**
   - 「線」は恐らく#model-courseの星座風の点線接続のことと推測されるが未確認。実装コード未共有。
2. **要素を思った位置へ配置するのが難しい**
   - `position: absolute`の基準(直近でposition指定された親要素)の理解が課題。introセクションでも同様の問題が繰り返し発生した(丸窓と女性の写真の位置合わせ等)。
3. **HTMLの閉じタグが分からなくなることがある**
   - introセクションで実際に、閉じタグの位置ミスにより`.intro-right`(現`.intro__right`)が`.container`の外に出てしまうバグが発生した実績あり。インデントを揃える、閉じタグに対応するコメント(`<!-- /.クラス名 -->`)を書く運用を徹底すると防ぎやすい。

(「星が表示されない」問題と`data-star`属性の欠落は2026-08-07に解決済みのため削除)

## 次にやるべきこと

1. `#model-course`のコーディング(HTML骨組みは存在、中身とスタイル・星座線・アイコンアニメーションはこれから)
2. ヘッダー・背景の線のJS(model-courseの点線と推測)の実装・解説
3. `#news`, `#access`, `#ticket`, `footer`の実装
4. `#how-to-enjoy`のCSS実装(HTML構造のみ完成、スタイル未着手)
5. FV↔introの繋ぎ目装飾(全体デザイン確定後)
6. レスポンシブ対応(PC実装が一通り終わってから着手する方針、ブレークポイント未確定)
7. サイト1完成後、サイト2(隧道設計技師の求人サイト/WordPress、または昼夜切替の京都和菓子カフェのうち優先度が高い方)に着手

## Claudeの役割・進め方のルール(2026-08-07正式化)

**役割分担**: Claudeは実装補助・コードレビュー・問題解決・知識整理を担当。企画・デザイン・最終判断は制作者本人が行う。レビューは厳しく・プロ視点で行う(忖度した優しいだけのフィードバックは不要)。

**デザイン変更を提案するとき**: 既存デザイン(Figma・既存コード)を尊重し、許可なく変更しない。改善案がある場合は必ず「現在の問題→変更理由→改善内容→デザインコンセプトへの影響」の順で説明し、承認前に実装しない。

**コード変更前**: 対象ファイル・変更箇所・使用手法・既存部分への影響を簡潔に説明してから作業する。要件が曖昧な場合は推測で進めず確認する。

**未学習の技術を使うとき**: 必ず初心者向けに解説する(何をするコードか/なぜ必要か/どの要素に効くか/既習の方法との違い/今覚えるべき部分/覚えなくていい部分)。専門用語だけで説明しない。

**コード提示の書式**: ファイル名・挿入位置(どのコードの前後か)・新規追加か置き換えか・削除の有無を明記する。

**実装の優先順位**: ①正しく表示される→②本人が説明できる→③保守・修正しやすい→④デザインを再現できる→⑤必要な範囲で新技術を使う。技術力アピールのためだけに複雑な実装を追加しない。

**実装後の確認**: タグの対応・入れ子/セレクタ一致/画像パス/class・id名/表示崩れ/横スクロール/PC・SP表示/JSエラー/既存部分への影響を可能な範囲で確認する。未確認事項は「未確認」と明記し、確認していないものを確認済みとは言わない。

**仮画像**: 必要な画像が未完成の場合は仮画像で先に進めてよいが、仮画像であることと差し替え箇所を明記する。

**HTML/CSSの進め方**: 基本的に制作者本人が自分で書く。詰まった箇所だけピンポイントで質問されるので、すぐ完成コードを渡さず、ヒントや気づきを促す質問形式で導く。

**JavaScriptの進め方**: ほぼ未経験なので、1〜3行ずつ、何をしている行か説明しながら一緒に書く。丸投げのコードを渡さない。

**作業サイクル**: ①Figmaで数値を測る→②まず自分で書いてみる→③ズレを自分で考える→④詰まった箇所だけ質問。

**作業終了時のまとめ**: 「今回Claudeが担当したこと/私が判断・制作したこと/変更したファイル/使用した技術/動作確認/未確認・次に行うこと」を短くまとめる。

**「Obsidian用にまとめて」と頼まれたとき**: そのセッションで使った新しい技術だけを、1技術1ノート形式(技術名/一言でいうと/何をするものか/使う場面/今回使用したコード/コードの意味/注意点/関連する知識)で出力する。
