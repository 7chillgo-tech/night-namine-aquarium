## 最優先ルール:コードは書かない、教えるだけ

**Claudeは、ユーザーに代わってコードを直接書いたり編集したりしない。**
就職活動用ポートフォリオであり、実装は必ずユーザー自身の手で行う必要がある。

Claudeの役割は以下のみ:
- 実装方法の説明(どのファイルの、どこに、何を書くべきか)
- なぜそのコードが必要かの解説(初心者向け)
- ユーザーが書いたコードのレビュー・間違いの指摘(直接修正はしない、
  「ここをこう直すといい」と伝えるだけ)
- 問題が起きたときの原因調査・説明

ファイルの編集・作成・実行は、明示的に「代わりに書いて」と頼まれない限り、
絶対に行わない

## 回答・実装時のルール

1. **既存デザインを尊重する**:Figmaや既存コードのデザインを、許可なく変更しない。
   改善案がある場合は、実装前に「現在の問題→変更理由→改善効果→デザインへの影響」を
   説明してから、ユーザーの承認を得ること。

2. **実装前に確認する**:コードを変更する前に、対象ファイルと関連コードを確認し、
   「変更するファイル/部分/方法/既存部分への影響」を短く説明してから作業する。

3. **学習を妨げない**:未学習のコードを使う場合は、初心者向けに
   「何をするコードか/なぜ必要か/どの要素に効くか/既習との違い/今覚えるべき部分/
   覚えなくていい部分」を実際のHTML/CSSと結びつけて説明する。

4. **コードの場所を明確にする**:提示するコードには「ファイル名/どこに入れるか/
   新規か置き換えか/削除するコードの有無」を必ず明記する。

5. **必要以上に複雑にしない**:優先順位は「正しく表示される→説明できる→
   保守しやすい→デザインを再現できる→必要な範囲で新技術を使う」。

6. **必ず確認する**:実装後は開始・終了タグ、入れ子、セレクタ一致、画像パス、
   class/id名、横スクロール、表示崩れなどを確認し、未確認事項は正直に伝える。

7. **仮画像を許可する**:画像が未完成でも仮画像で先に進めてよい。ただし仮画像である
   ことと差し替え場所を明記する。

8. **作業終了時にまとめる**:「Claudeが担当したこと/ユーザーが判断・制作したこと/
   変更したファイル/使用技術/動作確認/未確認・次にやること」を記録する。

---

# 現在の進め方(2026-08-17時点・星座線アニメーション実装後)

**全セクションのHTML/CSSは実装済み。レスポンシブ対応・星座線まわりのJS実装も完了、
`main`にマージ・公開済み。** 締切は2026-08-25(TOPページ公開)。

## 直近のTODO
1. **新しく追加した演出の実機ブラウザでの動作確認** ← 今ここ
   ヘッドレスブラウザでの静止画確認のみ済み。以下は実際にスクロールしながらの
   確認がまだ:
   - 星座線がスクロール量に応じて伸びていく動き(FV〜TICKET)
   - 星の瞬き
   - INTRO・HOW TO ENJOY・MODEL COURSEのフェードイン
   - FV・MODEL COURSE・TICKET背景の横パララックス
   - MODEL COURSE・TICKETのカーソル追従の光
2. **写真のグロー(box-shadowでにじませる)**:`.photo-glow` / `.photo-glow-small`
   クラスはまだ未実装(下記「01セクション」参照)。
3. **小さいディテール写真**:大きい丸窓写真の右下にタコ・うつぼカット(150px前後)を
   追加する案も未着手。

## 後回しでよいもの(優先度低・時間が合えば)
- 背景画像3枚(`fv.jpg` / `modelcourse.jpg` / `ticket.jpg`)のさらなる圧縮
  (既に大幅圧縮済みだが、書き出し目安の400KB前後までは届いていない)
- スタイルガイドとコードの数値のズレの解消 → **サイト完成後にまとめて対応する方針**

## 見送り・不採用が決まったもの
- footerの reveal 演出(`position: sticky`)→ **実装しない方針に決定**(保留ではなく不採用)
- 光の粒子(パーティクル)演出(`.intro-particle`)→ 一度実装したが、写真の縁から
  はみ出す配置が原因で横スクロールバグを誘発したことと、星座線と役割が重複して
  見えたため、**星座線1本に絞る方針に決定・削除済み**(2026-08-17)
- `.intro-how-to-bg`のグラデーション(`--color-blue-300`/`--color-blue-400`)を
  暗くする案 → 一度試したが、**元の色(`#9CC5CA` / `#DFE6E1`)に戻す方針に決定**
  (2026-08-17。「01セクション」の色指定は変更なしが確定)

## 完了したもの
- 施設名表記の統一(「なみね水族館」)→ 修正済み
- レスポンシブ対応 → 完了
- 目視チェック(スマホ実機/下層4ページ/1440px以上/ハンバーガー不透明度)→ OK
- 星座線の通過点座標確定・なめらかな曲線接続(コミット `75bb99f`)
- スクロール演出一式(2026-08-17、`main`にマージ済み・コミット `ba1dc4c`):
  - 星座線をスクロール位置に応じて伸ばす(`stroke-dasharray`/`stroke-dashoffset`
    + `getPointAtLength`で実座標から進み具合を計算)・星の瞬き(CSS `@keyframes`)
  - INTRO・HOW TO ENJOY・MODEL COURSEのスクロールフェードイン
    (`IntersectionObserver` + `data-reveal`属性)
  - FV・MODEL COURSE・TICKET背景の横方向パララックス(縦方向は`cover`指定だと
    余白がほぼ0の画像があり、隙間が出るバグになったため横方向に変更)
  - MODEL COURSE・TICKETでのカーソル追従の光(`pointermove` + CSS変数)

## 星座線(コンセプト装飾)の仕組み
- 「かえりみち、寄り道」というコンセプトを、ページを下にスクロールする中で
  ゴールドの線が写真の裏を通って星々を繋いでいく演出として実装している
- `#constellation-svg`(1枚のSVG、position: absolute, z-index: 1, pointer-events: none)が
  `[data-star]`属性を持つ要素の座標を取得し、JSで自動的にpathを描画する(`js/script.js`内 `drawConstellation()`)
- 線は必ず写真の"裏"を通す。線より手前に出したい要素(写真・テキストのまとまり)は
  個別に `position: relative; z-index: 2;` を指定する。**`main > section` のような
  セクション全体への一括z-index指定は絶対にしない**(背景ごと線が隠れるバグの原因になった実績あり)
- フォント読み込み完了後(`document.fonts.ready`)とresize時(debounce付き)に再描画する

## 01セクション:今回実装すべき内容

### 1. 背景グラデーション ✅ 完了(色は変更なしで確定)
`#intro` の `radial-gradient` の中心座標を、丸窓写真(`.intro-image`)の中心と
正確に一致させる。色は `#52A1B1 → #9CC5CA → #DFE6E1 → #F3F0E8`。
(2026-08-17:一度`--color-blue-300`/`--color-blue-400`を暗くする案を試したが、
元のこの色に戻す方針に決定。以後この項目は変更しない)

### 2. 写真のグロー(にじみ)
写真の縁を背景に馴染ませるため、box-shadowでグローを付ける。

```css
.photo-glow {
  box-shadow: 0 0 120px 25px rgba(82, 161, 177, 0.35);
}
.photo-glow-small {
  /* 小さい円は面積比で弱く見えるため、相対的に強めにする */
  box-shadow: 0 0 90px 20px rgba(82, 161, 177, 0.4);
}
```

大きい円(`.intro-image`)には `.photo-glow`、小さいディテール円には
`.photo-glow-small` を使う。今後増える写真にもこの2クラスを使い回す。

### 3. 光の粒子(パーティクル) ❌ 不採用(下記「見送り・不採用が決まったもの」参照)
一度JSで実装したが、写真の縁からはみ出す配置が横スクロールバグの原因になったことと、
星座線と役割が重複して見えたため不採用に決定(2026-08-17)。今後もこの項目は
実装しない方針。

### 4. 小さいディテール写真
大きい丸窓写真の右下に、直径150px前後の小さい円(タコ・うつぼが写っているカット)を
追加する。星座線がこの円の裏を通るように、z-index順序に注意する。

## HTMLの軽微な不備
`.container`閉じ忘れなどが見つかった場合は都度直してよい。