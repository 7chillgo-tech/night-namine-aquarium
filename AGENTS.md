# 夜の波音水族館 コーディング引き継ぎメモ

最終更新: セッション時点

## プロジェクト概要

- 就職活動用ポートフォリオサイト(2サイト作成予定のうちの1つ目)
- 架空の市営水族館「なみね水族館」の夜間営業リニューアルLP
- コンセプト:「帰り道に寄り道できる、何度でも来れて休める、癒される水族館」
- 都会型ナイトアクアリウムとの差別化:非日常より日常の延長、少人数のリピーター獲得、特別すぎない親しみやすさ
- 制作者の学習状況:HTML/CSS(XHTML・HTML5)は職業訓練で習得済み、JavaScriptはほぼ未経験(Progateで変数のみ独学)
- 締切:8/25までにサイト1・サイト2をそれぞれTOPページだけでも公開状態にする

## 使用ツール・環境

- エディタ: VS Code
- バージョン管理: GitHub Desktop → GitHub Pages
- デザイン: Figma
- リポジトリ: `7chillgo-tech/night-namine-aquarium`
- 公開URL: `https://7chillgo-tech.github.io/night-namine-aquarium/`

## デザインの決定事項

- フォント: 見出し・コピー系は明朝系検討中/本文は Zen Kaku Gothic New、英字ラベルは DM Sans
- 配色: 深紺 × クリーム(#183B4E, #F3F0E8 など)、グラデーション背景を導入(introで実装済み)
- モチーフ: 泡(気泡)装飾を採用予定(当初のキラキラ装飾から変更)。星座モチーフ(点線で繋ぐ)はモデルコースセクションで使用
- 写真の切り抜きルール:
  - 円形 = 生き物・展示にフォーカスする瞬間
  - ブロブ(いびつな角丸) = くつろぐ空間・軽食
  - フルブリード四角 = 情報・全景セクション
- 画像命名規則: `[セクション名]_[内容].拡張子`、小文字ケバブケース、日本語・スペース・バージョン語(final/v2等)禁止

## コーディング済み箇所

- **header**: `position: fixed`。スクロール量0で透明、スクロールすると`is-scrolled`クラスが付与され背景色(`rgba(24,59,78,0.6)`)+`backdrop-filter: blur(10px)`が付く。以前あった「スクロール中は消える」演出は可読性の問題で削除済み。
- **#fv**: 完成。`height:100vh`、背景`fv.png`。コピー「かえりみち、海に寄り道。」「島の海と、ひと休み。」、右にイベントロゴ・開催期間画像。FVとintroの繋ぎ目(波デザイン)は保留中(全体デザイン見直し中のため)。
- **#intro**: 完成。左にラベル(h2)+見出し(h3)+本文+ボタン(`.button`)、右に丸窓写真(`.intro-image`)+女性の写真(`.intro-image2`、Figmaで個別トリミング書き出し済み、`object-fit: cover; object-position: top;`)。背景は`radial-gradient`。アンカージャンプ時にFVが透けて見える問題は`scroll-margin-top: 0`で解決済み。
- **#how-to-enjoy**: コーディング途中(今セッションで完了と報告あり、詳細コード未共有)。
- **#news**: デザイン変更を実施(詳細未共有)。
- **#model-course**: HTML骨組みのみ存在。中身の実装はこれから。
  - Figmaデザイン: ラベル「03 | MODEL COURSE」、ハイライトされたコピー「会社帰りに、ひとり時間。」、ボタン「夜の過ごし方を見る→」、夜景の背景画像、時系列4ステップ(18:30/18:45/19:15/20:00)を線画アイコン(人物・魚・グラス・椅子)で表示し、点線(星座のように)で繋ぐ演出。
- **#news, #access, #ticket, footer**: 未実装、または一部進行中(newsはデザイン変更のみ)。

## 見出し構造のルール(合意済み)

```
h1: ヘッダーロゴ
h2: 各セクションのラベル(例: "01 | INTRODUCTION", "02 | HOW TO ENJOY")
h3: 各セクション内の具体的な見出し(例: "水槽の前で、ひとり時間。")
```
同じ階層の兄弟見出しは同じレベルのタグに揃える。

## 現在つまずいていること

1. **ヘッダーと背景にある線のJavaScriptが理解できていない**
   - 「線」は恐らく#model-courseの星座風の点線接続のことと推測されるが未確認。実装コード未共有。
2. **星が表示されない**
   - 過去のセッションでの既知の失敗パターン: `data-star`属性の欠落、絶対配置のための要素階層の誤り、z-indexをセクション単位ではなく個々のコンテンツ要素に当てるべきところを誤って適用、SVGの配置フォルダ違い。今回も同様の原因の可能性が高いが、実コード未確認。
3. **要素を思った位置へ配置するのが難しい**
   - position: absolute の基準(直近のposition指定された親要素)の理解が課題。introセクションでも同様の問題が繰り返し発生した(丸窓と女性の写真の位置合わせ等)。
4. **HTMLの閉じタグが分からなくなることがある**
   - introセクションで実際に、閉じタグの位置ミスにより`.intro-right`が`.container`の外に出てしまうバグが発生した実績あり。インデントを揃える、閉じタグに対応するコメント(`<!-- /.クラス名 -->`)を書く運用を徹底すると防ぎやすい。

## 次にやるべきこと

1. `#how-to-enjoy`と`#news`の最新コードを確認(このセッションでは未共有のため要確認)
2. `#model-course`のコーディング(HTML骨組みは存在、中身とスタイル・星座線・アイコンアニメーションはこれから)
3. 星が表示されない問題の原因調査(実コードとスクリーンショットが必要)
4. ヘッダー・背景の線のJS(model-courseの点線と推測)の実装・解説
5. `#news`, `#access`, `#ticket`, `footer`の実装
6. FV↔introの繋ぎ目装飾(全体デザイン確定後)
7. レスポンシブ対応(PC実装が一通り終わってから着手する方針)
8. サイト1完成後、サイト2(隧道設計技師の求人サイト/WordPress、または昼夜切替の京都和菓子カフェのうち優先度が高い方)に着手

## 引き継ぎ後の進め方の希望(合意済みのスタイル)

- HTML/CSS: 制作者がまず自分で書く。詰まった箇所をピンポイントで質問してもらい、すぐ答えを渡さずヒント→気づきを促す形で進める。完成コードを先に渡さない。
- JavaScript: ほぼ未経験のため、1〜3行ずつ、何をしている行か説明しながら一緒に書く。丸投げしない。
- デザインは既存のFigma案を尊重し、大きく変更する場合は事前に理由と影響を説明してから提案する。
- 実装前に対象ファイル・変更範囲・使用手法・既存部分への影響を簡潔に説明してから着手する。
- コード提示時はファイル名、挿入位置、新規/置き換えの別を明記する。
- 実装後の動作確認は、実際に確認できたことと未確認のことを分けて明記する。